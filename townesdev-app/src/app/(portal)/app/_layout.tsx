// implements the layout for all /portal routes

// import { ReactNode } from "react";
// import { redirect } from "next/navigation";
// import { getServerSession } from "next-auth"; // uncomment if using next-auth
//import { ClientSidebar } from "@/components/ClientSidebar";  // import from components

// export default async function PortalLayout({ children }: { children: ReactNode }) {
//   const session = await getServerSession();
//   if (!session) redirect("/login");
//   return (
//     <div className="flex min-h-screen">
//       <ClientSidebar />
//       <main className="flex-1 p-6">{children}</main>
//     </div>
//   );
// }

// Note: The above code is commented out as a placeholder. Uncomment and modify as needed based on authentication and layout requirements later on.